import 'package:flutter/widgets.dart';

class CodeSuccess extends StatelessWidget {
  const CodeSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}